const square = function(x) {
    return x * x;
};

// const sqaureArrow = (x) => {
//     return x * x;
// }; 

const sqaureArrow = (x) => x * x;
console.log(sqaureArrow(9))

const getFirstName = (name) => name.split(' ')[0]
console.log(getFirstName('Shrobon Biswas'))